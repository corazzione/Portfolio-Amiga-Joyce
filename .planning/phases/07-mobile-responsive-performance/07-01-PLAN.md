---
phase: 07-mobile-responsive-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/ServicesSection.tsx
  - components/sections/PortfolioSection.tsx
  - components/sections/FAQSection.tsx
  - components/sections/BlogSection.tsx
  - components/sections/ProofStripSection.tsx
  - app/layout.tsx
  - app/portfolio/page.tsx
  - app/contato/page.tsx
autonomous: true
requirements: [RESP-01, RESP-02, RESP-03, RESP-04, RESP-05]

must_haves:
  truths:
    - "All grid sections have grid-cols-1 base class before responsive variants"
    - "Section headings scale down on mobile (text-2xl or text-3xl base before sm:/lg: variants)"
    - "ProofStripSection logos wrap on narrow screens instead of causing horizontal overflow"
    - "ServicesSection header wraps to stacked layout on mobile"
    - "Sub-page headings (portfolio, contato) scale down on mobile"
    - "No horizontal overflow at 375px viewport width"
  artifacts:
    - path: "components/sections/ServicesSection.tsx"
      provides: "Responsive heading + header flex-wrap"
      contains: "text-2xl sm:text-4xl"
    - path: "components/sections/PortfolioSection.tsx"
      provides: "Responsive heading"
      contains: "text-2xl sm:text-4xl"
    - path: "components/sections/FAQSection.tsx"
      provides: "Responsive heading"
      contains: "text-3xl lg:text-5xl"
    - path: "components/sections/ProofStripSection.tsx"
      provides: "Logo flex-wrap for mobile"
      contains: "flex-wrap"
    - path: "app/layout.tsx"
      provides: "Horizontal overflow prevention"
      contains: "overflow-x-hidden"
  key_links:
    - from: "app/layout.tsx"
      to: "all pages"
      via: "overflow-x-hidden on html element"
      pattern: "overflow-x-hidden"
---

<objective>
Audit and fix responsive classes across all section components and sub-pages so every layout reflows correctly at 375px mobile viewport.

Purpose: Fulfill RESP-01 (single-column stacks), RESP-02 (services grid breakpoints — already correct, verify), RESP-03 (bento grid — already correct, verify), RESP-04 (hero text — already has mobile base, verify), RESP-05 (nav — already correct, verify). Primary work is typography scaling and flex-wrap fixes on 4-5 components.

Output: All section components have mobile-first responsive classes; no horizontal overflow at 375px.
</objective>

<execution_context>
@C:/Users/coraz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/coraz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mobile-responsive-performance/07-CONTEXT.md
@.planning/phases/07-mobile-responsive-performance/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix typography scaling and flex-wrap on section components</name>
  <files>
    components/sections/ServicesSection.tsx,
    components/sections/PortfolioSection.tsx,
    components/sections/FAQSection.tsx,
    components/sections/BlogSection.tsx,
    components/sections/ProofStripSection.tsx
  </files>
  <read_first>
    components/sections/ServicesSection.tsx,
    components/sections/PortfolioSection.tsx,
    components/sections/FAQSection.tsx,
    components/sections/BlogSection.tsx,
    components/sections/ProofStripSection.tsx,
    .planning/phases/07-mobile-responsive-performance/07-RESEARCH.md
  </read_first>
  <action>
Apply the following specific changes to each file:

**ServicesSection.tsx:**
1. Change heading `text-4xl` to `text-2xl sm:text-4xl` on the h2 element (line ~10: `<h2 className="font-switzer text-4xl ...">`)
2. Change the header container `flex items-start justify-between mb-12` to `flex flex-col sm:flex-row sm:items-start sm:justify-between gap-4 mb-12` so the heading and button stack vertically on mobile instead of side-by-side

**PortfolioSection.tsx:**
1. Change heading `text-4xl` to `text-2xl sm:text-4xl` on the h2 element (line ~17: `<h2 className="font-switzer text-4xl ...">`)

**FAQSection.tsx:**
1. Change heading `text-5xl` to `text-3xl lg:text-5xl` on the h2 element (line ~15: `<h2 className="font-switzer text-5xl ...">`)

**BlogSection.tsx:**
1. Change heading `text-4xl` to `text-2xl sm:text-4xl` on the h2 element (line ~8: `<h2 className="font-switzer text-4xl ...">`)

**ProofStripSection.tsx:**
1. Add `flex-wrap` to the client logos container (line ~28: `<div className="flex items-center gap-6">` becomes `<div className="flex items-center gap-3 sm:gap-6 flex-wrap justify-center">`)
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/ServicesSection.test.tsx __tests__/components/sections/FAQSection.test.tsx __tests__/components/sections/PortfolioSection.test.tsx __tests__/components/sections/BlogSection.test.tsx __tests__/components/sections/ProofStripSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ServicesSection.tsx contains `text-2xl sm:text-4xl`
    - ServicesSection.tsx contains `flex-col sm:flex-row`
    - PortfolioSection.tsx contains `text-2xl sm:text-4xl`
    - FAQSection.tsx contains `text-3xl lg:text-5xl`
    - BlogSection.tsx contains `text-2xl sm:text-4xl`
    - ProofStripSection.tsx contains `flex-wrap`
    - ProofStripSection.tsx contains `gap-3 sm:gap-6`
    - All 5 test files pass with exit code 0
  </acceptance_criteria>
  <done>All 5 section components have mobile-first typography and flex-wrap classes; existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sub-page headings and add overflow-x-hidden to layout</name>
  <files>
    app/portfolio/page.tsx,
    app/contato/page.tsx,
    app/layout.tsx
  </files>
  <read_first>
    app/portfolio/page.tsx,
    app/contato/page.tsx,
    app/layout.tsx
  </read_first>
  <action>
Apply the following specific changes:

**app/portfolio/page.tsx:**
1. Change h1 `text-5xl` to `text-3xl sm:text-5xl` (line ~60: `<h1 className="font-switzer text-5xl ...">`)

**app/contato/page.tsx:**
1. Change h1 `text-5xl` to `text-3xl sm:text-5xl` (line ~17: `<h1 className="font-switzer text-5xl ...">`)

**app/layout.tsx:**
1. Add `overflow-x-hidden` to the `<html>` element (NOT body — body overflow-hidden breaks sticky positioning). Change line ~18 from:
   `<html lang="pt-BR" className={` ` ${switzer.variable} ${publicSans.variable}` ` }>`
   to:
   `<html lang="pt-BR" className={` ` ${switzer.variable} ${publicSans.variable} overflow-x-hidden` ` }>`

Important: Apply overflow-x-hidden to `<html>` per Pitfall 5 in RESEARCH.md — applying to `<body>` breaks ProcessSection sticky positioning.
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/PortfolioPage.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/portfolio/page.tsx contains `text-3xl sm:text-5xl`
    - app/contato/page.tsx contains `text-3xl sm:text-5xl`
    - app/layout.tsx contains `overflow-x-hidden` on the html element className
    - app/layout.tsx does NOT contain overflow-x-hidden on the body element
    - PortfolioPage test passes with exit code 0
  </acceptance_criteria>
  <done>Sub-page headings scale on mobile; horizontal overflow prevented via html element; sticky positioning unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Verify RESP-02/03/04/05 classes and fix ServicesSection heading gap</name>
  <files>
    components/sections/ServicesSection.tsx
  </files>
  <read_first>
    components/sections/BentoSection.tsx,
    components/sections/ServicesSection.tsx,
    components/hero/HeroTextSelector.tsx,
    components/layout/TopNav.tsx,
    components/layout/BottomBar.tsx
  </read_first>
  <action>
Pre-planned audit results (confirmed by reading each file before this plan was written):

**RESP-02 — ServicesSection grid:** `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` already present on line 18. CORRECT — no change needed.

**RESP-03 — BentoSection grid:** `grid grid-cols-1 sm:grid-cols-3` already present on line 19. CORRECT — no change needed.

**RESP-04 — HeroTextSelector cycling text:** `text-2xl md:text-4xl` already present in HeroTextSelector.tsx. `text-2xl` is the mobile base. CORRECT — no change needed.

**RESP-05 — TopNav:** Design uses BottomBar for all mobile navigation; TopNav shows only logo + CTA button. Tagline subtitle already uses `hidden md:inline`. CORRECT — no change needed.

**RESP-05 — BottomBar safe area:** `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}` already present on the fixed bottom bar div. CORRECT — no change needed.

**ACTION — ServicesSection heading (MISSING):** Task 1 above applies `text-2xl sm:text-4xl` to the ServicesSection h2 and `flex-col sm:flex-row` to the header container. This task confirms those changes landed correctly by running grep assertions.

Run the following grep checks to confirm all RESP-02/03/04/05 classes are present:

```bash
grep -n "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4" components/sections/ServicesSection.tsx
grep -n "grid-cols-1 sm:grid-cols-3" components/sections/BentoSection.tsx
grep -n "text-2xl md:text-4xl" components/hero/HeroTextSelector.tsx
grep -n "hidden md:inline" components/layout/TopNav.tsx
grep -n "safe-area-inset-bottom" components/layout/BottomBar.tsx
grep -n "text-2xl sm:text-4xl" components/sections/ServicesSection.tsx
grep -n "flex-col sm:flex-row" components/sections/ServicesSection.tsx
```

All 7 greps must return a matching line. If any grep returns empty, fix the missing class in the indicated file before proceeding.
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && grep -n "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4" components/sections/ServicesSection.tsx && grep -n "grid-cols-1 sm:grid-cols-3" components/sections/BentoSection.tsx && grep -n "text-2xl md:text-4xl" components/hero/HeroTextSelector.tsx && grep -n "hidden md:inline" components/layout/TopNav.tsx && grep -n "safe-area-inset-bottom" components/layout/BottomBar.tsx && grep -n "text-2xl sm:text-4xl" components/sections/ServicesSection.tsx && grep -n "flex-col sm:flex-row" components/sections/ServicesSection.tsx && echo "ALL RESP-02/03/04/05 CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - ServicesSection.tsx contains `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (RESP-02 grid)
    - BentoSection.tsx contains `grid-cols-1 sm:grid-cols-3` (RESP-03)
    - HeroTextSelector.tsx contains `text-2xl md:text-4xl` (RESP-04)
    - TopNav.tsx contains `hidden md:inline` (RESP-05 desktop-only subtitle)
    - BottomBar.tsx contains `safe-area-inset-bottom` (RESP-05 safe area)
    - ServicesSection.tsx contains `text-2xl sm:text-4xl` (RESP-02 heading — fixed in Task 1)
    - ServicesSection.tsx contains `flex-col sm:flex-row` (RESP-02 header stack — fixed in Task 1)
    - All grep commands exit 0 and print matching lines
  </acceptance_criteria>
  <done>All RESP-02/03/04/05 responsive classes confirmed present in BentoSection, HeroTextSelector, TopNav, BottomBar, and ServicesSection. Requirements are verifiably satisfied.</done>
</task>

</tasks>

<verification>
- All existing tests pass: `npx vitest run`
- No horizontal overflow classes missing: grep for `text-4xl` and `text-5xl` in section components confirms all have mobile base sizes
</verification>

<success_criteria>
- Every section heading has a mobile-first text size (text-2xl or text-3xl base)
- ProofStripSection logos container has flex-wrap
- ServicesSection header stacks on mobile
- Sub-page headings scale down
- overflow-x-hidden on html element prevents any horizontal scroll
- BentoSection, HeroTextSelector, TopNav, BottomBar pass grep audit confirming RESP-03/04/05
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-mobile-responsive-performance/07-01-SUMMARY.md`
</output>
