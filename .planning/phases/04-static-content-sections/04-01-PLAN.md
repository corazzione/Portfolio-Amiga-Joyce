---
phase: 04-static-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/components/sections/PhotoStripSection.test.tsx
  - __tests__/components/sections/ManifestoSection.test.tsx
  - __tests__/components/sections/ProofStripSection.test.tsx
  - __tests__/components/sections/ServicesSection.test.tsx
  - __tests__/components/sections/ServiceCard.test.tsx
  - __tests__/components/sections/PortfolioSection.test.tsx
  - __tests__/components/sections/BlogSection.test.tsx
  - __tests__/components/sections/FAQSection.test.tsx
  - __tests__/components/sections/CTASection.test.tsx
  - __tests__/components/sections/FooterSection.test.tsx
  - components/sections/PhotoStripSection.tsx
  - components/sections/ManifestoSection.tsx
  - components/sections/ProofStripSection.tsx
  - components/sections/ServicesSection.tsx
  - components/sections/ServiceCard.tsx
autonomous: true
requirements:
  - SECT-01
  - SECT-02
  - SECT-03
  - SECT-04
  - SECT-05
  - ANIM-05

must_haves:
  truths:
    - "Photo strip renders a horizontal flex row of placeholder thumbnail divs below the hero"
    - "Manifesto section renders large centered Switzer text with fadeUp reveal"
    - "Proof strip shows gold 'Meu Portfolio' button, 3 avatar circles, and 4 client logo names as spans"
    - "Services section renders a 4-column grid with 7 service cards + 1 photo card"
    - "Service cards have whileHover scale effect via 'use client' ServiceCard component"
  artifacts:
    - path: "components/sections/PhotoStripSection.tsx"
      provides: "Horizontal photo strip with placeholder divs"
      contains: "aspect-[4/3]"
    - path: "components/sections/ManifestoSection.tsx"
      provides: "Centered manifesto text block"
      contains: "font-switzer"
    - path: "components/sections/ProofStripSection.tsx"
      provides: "CTA proof strip with button, avatars, logos"
      contains: "clientLogos"
    - path: "components/sections/ServicesSection.tsx"
      provides: "Services grid section"
      contains: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
    - path: "components/sections/ServiceCard.tsx"
      provides: "Individual service card with hover"
      contains: "use client"
  key_links:
    - from: "components/sections/ServicesSection.tsx"
      to: "components/sections/ServiceCard.tsx"
      via: "import ServiceCard"
      pattern: "import.*ServiceCard"
    - from: "components/sections/ServicesSection.tsx"
      to: "lib/data.ts"
      via: "import services"
      pattern: "import.*services.*from.*lib/data"
    - from: "components/sections/ProofStripSection.tsx"
      to: "lib/data.ts"
      via: "import clientLogos"
      pattern: "import.*clientLogos.*from.*lib/data"
---

<objective>
Create all 10 test stub files (Wave 0) and implement the first 4 homepage sections: PhotoStripSection, ManifestoSection, ProofStripSection, and ServicesSection with ServiceCard.

Purpose: Establish the test infrastructure for all Phase 4 sections upfront, then build the top portion of the homepage below the hero.
Output: 10 test files + 5 component files (4 sections + ServiceCard)
</objective>

<execution_context>
@E:/Fotógrafo/.claude/get-shit-done/workflows/execute-plan.md
@E:/Fotógrafo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-static-content-sections/04-CONTEXT.md
@.planning/phases/04-static-content-sections/04-RESEARCH.md
@.planning/phases/04-static-content-sections/04-VALIDATION.md

@lib/data.ts
@lib/animation-variants.ts
@components/motion/RevealWrapper.tsx
@components/motion/StaggerChildren.tsx
@__tests__/components/hero/HeroSection.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/data.ts:
```typescript
export interface Service {
  id: string
  title: string
  description: string
  icon: string
}

export interface ClientLogo {
  id: string
  name: string
  src: string
}

export const services: Service[]        // 7 items
export const clientLogos: ClientLogo[]  // 4 items: theo, Amsterdam, luminous, MILANO
```

From components/motion/RevealWrapper.tsx:
```typescript
export function RevealWrapper({ children, delay, className, variant }: {
  children: React.ReactNode
  delay?: number
  className?: string
  variant?: 'fadeUp' | 'scaleIn'
}): JSX.Element
```

From components/motion/StaggerChildren.tsx:
```typescript
export function StaggerChildren({ children, className }: {
  children: React.ReactNode
  className?: string
}): JSX.Element

export function StaggerItem({ children, className }: {
  children: React.ReactNode
  className?: string
}): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 10 section test stubs (Wave 0)</name>
  <files>
    __tests__/components/sections/PhotoStripSection.test.tsx
    __tests__/components/sections/ManifestoSection.test.tsx
    __tests__/components/sections/ProofStripSection.test.tsx
    __tests__/components/sections/ServicesSection.test.tsx
    __tests__/components/sections/ServiceCard.test.tsx
    __tests__/components/sections/PortfolioSection.test.tsx
    __tests__/components/sections/BlogSection.test.tsx
    __tests__/components/sections/FAQSection.test.tsx
    __tests__/components/sections/CTASection.test.tsx
    __tests__/components/sections/FooterSection.test.tsx
  </files>
  <read_first>
    __tests__/components/hero/HeroSection.test.tsx
    lib/data.ts
  </read_first>
  <action>
Create `__tests__/components/sections/` directory with 10 test files. All use the same `vi.mock('motion/react', ...)` pattern from `HeroSection.test.tsx`.

Each test file follows this template structure:

```typescript
import { render, screen } from '@testing-library/react'
import { describe, it, expect, vi } from 'vitest'

const { mockUseReducedMotion } = vi.hoisted(() => ({
  mockUseReducedMotion: vi.fn(() => false),
}))

vi.mock('motion/react', () => ({
  motion: {
    div: ({ children, ...props }: React.HTMLAttributes<HTMLDivElement> & { children?: React.ReactNode }) => (
      <div {...props}>{children}</div>
    ),
    // Add motion.button if FAQSection uses it
  },
  AnimatePresence: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  useReducedMotion: mockUseReducedMotion,
  useInView: vi.fn(() => true),
}))
```

Also mock RevealWrapper and StaggerChildren/StaggerItem to pass-through renders:

```typescript
vi.mock('@/components/motion/RevealWrapper', () => ({
  RevealWrapper: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}))

vi.mock('@/components/motion/StaggerChildren', () => ({
  StaggerChildren: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
  StaggerItem: ({ children }: { children: React.ReactNode }) => <div>{children}</div>,
}))
```

**Test cases per file (use `it.todo()` for all — components don't exist yet):**

1. `PhotoStripSection.test.tsx` — imports `PhotoStripSection` from `@/components/sections/PhotoStripSection`
   - `it.todo('renders photo strip container')`
   - `it.todo('renders placeholder thumbnail divs')`

2. `ManifestoSection.test.tsx` — imports from `@/components/sections/ManifestoSection`
   - `it.todo('renders manifesto text content')`

3. `ProofStripSection.test.tsx` — imports from `@/components/sections/ProofStripSection`
   - `it.todo('renders Meu Portfolio button')`
   - `it.todo('renders avatar circles')`
   - `it.todo('renders all 4 client logo names')`

4. `ServicesSection.test.tsx` — imports from `@/components/sections/ServicesSection`. Also mock `@/components/sections/ServiceCard`:
   ```typescript
   vi.mock('@/components/sections/ServiceCard', () => ({
     ServiceCard: ({ service }: { service: { title: string } }) => <div data-testid="service-card">{service.title}</div>,
   }))
   ```
   - `it.todo('renders Meus Servicos heading')`
   - `it.todo('renders 7 service cards from data')`
   - `it.todo('renders 1 photo card placeholder')`

5. `ServiceCard.test.tsx` — imports from `@/components/sections/ServiceCard`
   - `it.todo('renders service title')`
   - `it.todo('renders service description')`
   - `it.todo('renders service icon')`

6. `PortfolioSection.test.tsx` — imports from `@/components/sections/PortfolioSection`. Also mock `@/components/sections/PortfolioCard`:
   ```typescript
   vi.mock('@/components/sections/PortfolioCard', () => ({
     PortfolioCard: ({ caption }: { caption: string }) => <div data-testid="portfolio-card">{caption}</div>,
   }))
   ```
   - `it.todo('renders Trabalhos Selecionados heading')`
   - `it.todo('renders 6 portfolio items')`

7. `BlogSection.test.tsx` — imports from `@/components/sections/BlogSection`
   - `it.todo('renders 3 blog post titles')`
   - `it.todo('renders hr separators between posts')`

8. `FAQSection.test.tsx` — imports from `@/components/sections/FAQSection`. Add `import userEvent from '@testing-library/user-event'` for future click tests.
   - `it.todo('renders 4 FAQ questions')`
   - `it.todo('clicking a question shows answer')`
   - `it.todo('clicking same question hides answer')`
   - `it.todo('clicking different question closes previous')`

9. `CTASection.test.tsx` — imports from `@/components/sections/CTASection`
   - `it.todo('renders Transforme heading text')`
   - `it.todo('renders Entre em Contato link to /contato')`

10. `FooterSection.test.tsx` — imports from `@/components/sections/FooterSection`
    - `it.todo('renders Corazon brand name')`
    - `it.todo('renders MC copyright 2026')`
    - `it.todo('renders Criado por Markus Corazzione')`

All files use `it.todo()` — they will fail with "todo" status, not import errors. Do NOT import the actual component yet since files don't exist. Comment out the import line with a `// TODO: uncomment when component exists` note.
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "vi.mock.*motion/react" __tests__/components/sections/ returns 10 matches (one per file)
    - grep -r "vi.mock.*RevealWrapper" __tests__/components/sections/ returns matches in files that need it
    - grep -r "it.todo" __tests__/components/sections/ returns at least 20 matches across all files
    - All 10 test files exist in __tests__/components/sections/
  </acceptance_criteria>
  <done>All 10 test stub files exist in __tests__/components/sections/ with vi.mock patterns and it.todo test cases. Vitest recognizes all files without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PhotoStripSection, ManifestoSection, ProofStripSection</name>
  <files>
    components/sections/PhotoStripSection.tsx
    components/sections/ManifestoSection.tsx
    components/sections/ProofStripSection.tsx
    __tests__/components/sections/PhotoStripSection.test.tsx
    __tests__/components/sections/ManifestoSection.test.tsx
    __tests__/components/sections/ProofStripSection.test.tsx
  </files>
  <read_first>
    lib/data.ts
    components/motion/RevealWrapper.tsx
    __tests__/components/sections/PhotoStripSection.test.tsx
    __tests__/components/sections/ManifestoSection.test.tsx
    __tests__/components/sections/ProofStripSection.test.tsx
  </read_first>
  <action>
**PhotoStripSection.tsx** — Server Component (no 'use client'):

```tsx
import { RevealWrapper } from '@/components/motion/RevealWrapper'

export function PhotoStripSection() {
  return (
    <section className="py-16 px-6 overflow-hidden">
      <RevealWrapper variant="scaleIn">
        <div className="flex gap-4 overflow-x-auto max-w-7xl mx-auto">
          {Array.from({ length: 6 }).map((_, i) => (
            <div
              key={i}
              className="flex-shrink-0 w-[220px] aspect-[4/3] bg-dark/10 rounded-lg"
            />
          ))}
        </div>
      </RevealWrapper>
    </section>
  )
}
```

Key details:
- No 'use client' — Server Component
- 6 placeholder divs with `aspect-[4/3]`, `bg-dark/10`, `rounded-lg`
- `flex-shrink-0 w-[220px]` for fixed thumbnail width
- RevealWrapper with `variant="scaleIn"`
- `overflow-x-auto` on flex container, `overflow-hidden` on section

**ManifestoSection.tsx** — Server Component:

```tsx
import { RevealWrapper } from '@/components/motion/RevealWrapper'

export function ManifestoSection() {
  return (
    <section className="py-24 px-6 bg-bg text-center">
      <div className="max-w-4xl mx-auto">
        <RevealWrapper variant="fadeUp">
          <p className="font-switzer text-4xl md:text-6xl font-bold leading-tight tracking-tight text-dark">
            Cada imagem conta uma historia.{' '}
            <span className="text-dark/50">
              Cada projeto comeca com uma conversa.
            </span>
          </p>
        </RevealWrapper>
      </div>
    </section>
  )
}
```

Key details:
- `font-switzer text-4xl md:text-6xl font-bold`
- Second sentence in `text-dark/50` span for contrast
- `max-w-4xl` (not 7xl) for narrower text block
- RevealWrapper with default `variant="fadeUp"`

**ProofStripSection.tsx** — Server Component:

```tsx
import { RevealWrapper } from '@/components/motion/RevealWrapper'
import { clientLogos } from '@/lib/data'

export function ProofStripSection() {
  return (
    <section className="py-24 px-6">
      <RevealWrapper>
        <div className="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-8">
          {/* Gold CTA button */}
          <a
            href="#portfolio"
            className="bg-gold text-white px-8 py-3 rounded-full font-switzer font-semibold text-sm tracking-wide hover:opacity-90 transition-opacity"
          >
            Meu Portfolio
          </a>

          {/* Avatar cluster */}
          <div className="flex items-center">
            {[0, 1, 2].map((i) => (
              <div
                key={i}
                className="w-9 h-9 rounded-full bg-dark/20 border-2 border-bg -ml-2 first:ml-0"
              />
            ))}
          </div>

          {/* Client logos as text spans */}
          <div className="flex items-center gap-6">
            {clientLogos.map((logo) => (
              <span
                key={logo.id}
                className="font-switzer font-semibold text-sm text-dark/40 tracking-widest uppercase"
              >
                {logo.name}
              </span>
            ))}
          </div>
        </div>
      </RevealWrapper>
    </section>
  )
}
```

Key details:
- Client logos rendered as `<span>{logo.name}</span>` — NOT img or next/image
- 3 avatar circles with `w-9 h-9 rounded-full bg-dark/20 border-2 border-bg -ml-2 first:ml-0`
- `bg-gold text-white px-8 py-3 rounded-full` for gold CTA button
- Imports `clientLogos` from `@/lib/data`

**Then update the 3 corresponding test files:**

Uncomment the component imports and convert `it.todo()` to real test implementations:

- PhotoStripSection.test.tsx: render, check for `aspect-[4/3]` class presence via container query, or check 6 placeholder divs rendered
- ManifestoSection.test.tsx: `screen.getByText(/Cada imagem/i)` exists
- ProofStripSection.test.tsx: `screen.getByText(/Meu Portfolio/i)` for button, check all 4 logo names present (`screen.getByText('theo')`, `screen.getByText('Amsterdam')`, etc.)
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/PhotoStripSection.test.tsx __tests__/components/sections/ManifestoSection.test.tsx __tests__/components/sections/ProofStripSection.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "RevealWrapper" components/sections/PhotoStripSection.tsx returns match
    - grep "aspect-\[4/3\]" components/sections/PhotoStripSection.tsx returns match
    - grep "scaleIn" components/sections/PhotoStripSection.tsx returns match
    - grep "font-switzer" components/sections/ManifestoSection.tsx returns match
    - grep "text-4xl" components/sections/ManifestoSection.tsx returns match
    - grep "fadeUp" components/sections/ManifestoSection.tsx returns match
    - grep "clientLogos" components/sections/ProofStripSection.tsx returns match
    - grep "logo.name" components/sections/ProofStripSection.tsx returns match
    - grep "Meu Portfolio" components/sections/ProofStripSection.tsx returns match
    - grep "bg-gold" components/sections/ProofStripSection.tsx returns match
    - grep "rounded-full bg-dark/20" components/sections/ProofStripSection.tsx returns match
    - None of the 3 component files contain 'use client'
    - All 3 test files pass vitest
  </acceptance_criteria>
  <done>PhotoStripSection, ManifestoSection, and ProofStripSection render correctly as Server Components with all specified content. Tests pass for all 3.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ServicesSection + ServiceCard with hover</name>
  <files>
    components/sections/ServicesSection.tsx
    components/sections/ServiceCard.tsx
    __tests__/components/sections/ServicesSection.test.tsx
    __tests__/components/sections/ServiceCard.test.tsx
  </files>
  <read_first>
    lib/data.ts
    components/motion/StaggerChildren.tsx
    __tests__/components/sections/ServicesSection.test.tsx
    __tests__/components/sections/ServiceCard.test.tsx
  </read_first>
  <action>
**ServiceCard.tsx** — Client Component ('use client' required for whileHover):

```tsx
'use client'

import { motion } from 'motion/react'
import type { Service } from '@/lib/data'

export function ServiceCard({ service }: { service: Service }) {
  return (
    <motion.div
      className="bg-white rounded-2xl p-6 shadow-sm cursor-default"
      whileHover={{ scale: 1.02, y: -4 }}
      transition={{ duration: 0.2, ease: [0.22, 1, 0.36, 1] }}
    >
      <span className="text-2xl">{service.icon}</span>
      <h3 className="font-switzer text-lg font-bold mt-4 mb-2">{service.title}</h3>
      <p className="text-dark/70 text-sm leading-relaxed">{service.description}</p>
    </motion.div>
  )
}
```

Key details:
- `'use client'` at top — required for `whileHover`
- Import `motion` from `'motion/react'` (never framer-motion)
- `whileHover={{ scale: 1.02, y: -4 }}` with `transition={{ duration: 0.2, ease: [0.22, 1, 0.36, 1] }}`
- `bg-white rounded-2xl p-6 shadow-sm` card styling
- Takes `{ service: Service }` prop

**ServicesSection.tsx** — Server Component (no 'use client'):

```tsx
import { StaggerChildren, StaggerItem } from '@/components/motion/StaggerChildren'
import { ServiceCard } from './ServiceCard'
import { services } from '@/lib/data'

export function ServicesSection() {
  return (
    <section id="services" className="py-24 px-6">
      <div className="max-w-7xl mx-auto">
        <div className="flex items-start justify-between mb-12">
          <h2 className="font-switzer text-4xl font-bold tracking-tight">Meus Servicos</h2>
          <a
            href="#services"
            className="bg-gold text-white px-6 py-2 rounded-full font-switzer text-sm font-semibold hover:opacity-90 transition-opacity"
          >
            Ver Todos
          </a>
        </div>
        <StaggerChildren className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {services.map((s) => (
            <StaggerItem key={s.id}>
              <ServiceCard service={s} />
            </StaggerItem>
          ))}
          <StaggerItem>
            <div className="bg-dark rounded-2xl aspect-[3/4] flex items-end p-6">
              <span className="text-white/60 font-switzer text-sm">Foto Card</span>
            </div>
          </StaggerItem>
        </StaggerChildren>
      </div>
    </section>
  )
}
```

Key details:
- No 'use client' — Server Component; ServiceCard handles its own client boundary
- Imports `services` from `@/lib/data` — maps 7 service cards
- 8th card is a dark photo placeholder: `bg-dark rounded-2xl aspect-[3/4]`
- `StaggerChildren` wraps grid, `StaggerItem` wraps each card
- Grid classes: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6`
- Header row: "Meus Servicos" left + "Ver Todos" gold button right

**Then update the 2 test files:**

- ServicesSection.test.tsx: Uncomment import. Test that "Meus Servicos" heading renders, 7 service card test-ids exist, photo card placeholder exists
- ServiceCard.test.tsx: Uncomment import. Test renders service title, description, and icon from a mock service object `{ id: 'test', title: 'Test Service', description: 'Test desc', icon: '📷' }`
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/ServicesSection.test.tsx __tests__/components/sections/ServiceCard.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "'use client'" components/sections/ServiceCard.tsx returns match
    - grep "whileHover" components/sections/ServiceCard.tsx returns match
    - grep "scale: 1.02" components/sections/ServiceCard.tsx returns match
    - grep "motion/react" components/sections/ServiceCard.tsx returns match
    - grep "StaggerChildren" components/sections/ServicesSection.tsx returns match
    - grep "StaggerItem" components/sections/ServicesSection.tsx returns match
    - grep "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4" components/sections/ServicesSection.tsx returns match
    - grep "ServiceCard" components/sections/ServicesSection.tsx returns match
    - grep "services" components/sections/ServicesSection.tsx returns match
    - grep "bg-dark rounded-2xl" components/sections/ServicesSection.tsx returns match (photo card)
    - ServicesSection.tsx does NOT contain 'use client'
    - Both test files pass vitest
  </acceptance_criteria>
  <done>ServicesSection renders a 4-column grid with 7 service cards from data.ts + 1 dark photo card. ServiceCard is a 'use client' component with whileHover scale:1.02. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run __tests__/components/sections/PhotoStripSection.test.tsx __tests__/components/sections/ManifestoSection.test.tsx __tests__/components/sections/ProofStripSection.test.tsx __tests__/components/sections/ServicesSection.test.tsx __tests__/components/sections/ServiceCard.test.tsx --reporter=verbose
</verification>

<success_criteria>
- All 10 test stub files exist in __tests__/components/sections/
- PhotoStripSection renders horizontal flex strip with 6 aspect-[4/3] placeholder divs, scaleIn reveal
- ManifestoSection renders centered Switzer text with fadeUp reveal
- ProofStripSection renders gold "Meu Portfolio" button, 3 avatar circles, 4 client logo spans
- ServicesSection renders 4-column grid with 7 ServiceCard components + 1 dark photo card
- ServiceCard is 'use client' with whileHover={{ scale: 1.02, y: -4 }}
- All 5 implemented component tests pass
- Remaining 5 test files have it.todo() stubs
</success_criteria>

<output>
After completion, create `.planning/phases/04-static-content-sections/04-01-SUMMARY.md`
</output>
