---
phase: 04-static-content-sections
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - components/sections/CTASection.tsx
  - components/sections/FooterSection.tsx
  - app/page.tsx
  - __tests__/components/sections/CTASection.test.tsx
  - __tests__/components/sections/FooterSection.test.tsx
autonomous: true
requirements:
  - SECT-09
  - SECT-10

must_haves:
  truths:
    - "CTA section renders dark background with 'Transforme Suas Ideias' heading and 'Entre em Contato' gold button linking to /contato"
    - "Footer renders white card on beige bg with Corazon brand, social links, nav links, 'Criado por Markus Corazzione', and 'MC. 2026'"
    - "page.tsx imports and renders all 10 sections plus HeroSection in order, with Phase 5 placeholder anchors"
    - "npm run build passes with zero errors"
  artifacts:
    - path: "components/sections/CTASection.tsx"
      provides: "Final CTA section"
      contains: "bg-dark"
    - path: "components/sections/FooterSection.tsx"
      provides: "Footer with white card"
      contains: "bg-white rounded-3xl"
    - path: "app/page.tsx"
      provides: "Complete homepage with all sections"
      contains: "FooterSection"
  key_links:
    - from: "app/page.tsx"
      to: "components/sections/PhotoStripSection.tsx"
      via: "import PhotoStripSection"
      pattern: "import.*PhotoStripSection"
    - from: "app/page.tsx"
      to: "components/sections/FooterSection.tsx"
      via: "import FooterSection"
      pattern: "import.*FooterSection"
    - from: "app/page.tsx"
      to: "components/hero/HeroSection.tsx"
      via: "import HeroSection (existing)"
      pattern: "import.*HeroSection"
---

<objective>
Implement CTASection and FooterSection, then wire all 10 sections into app/page.tsx with Phase 5 placeholder anchors. Verify npm run build passes.

Purpose: Complete the homepage end-to-end — every section from hero to footer is present and the build is clean.
Output: 2 new component files + updated page.tsx, build passes
</objective>

<execution_context>
@E:/Fotógrafo/.claude/get-shit-done/workflows/execute-plan.md
@E:/Fotógrafo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-static-content-sections/04-CONTEXT.md
@.planning/phases/04-static-content-sections/04-RESEARCH.md
@.planning/phases/04-static-content-sections/04-01-SUMMARY.md
@.planning/phases/04-static-content-sections/04-02-SUMMARY.md

@app/page.tsx
@lib/data.ts

<interfaces>
<!-- Components created in 04-01 and 04-02 that page.tsx will import -->

From components/sections/ (04-01):
```typescript
export function PhotoStripSection(): JSX.Element
export function ManifestoSection(): JSX.Element
export function ProofStripSection(): JSX.Element
export function ServicesSection(): JSX.Element
```

From components/sections/ (04-02):
```typescript
export function PortfolioSection(): JSX.Element
export function BlogSection(): JSX.Element
export function FAQSection(): JSX.Element
```

From components/hero/HeroSection.tsx (existing):
```typescript
export function HeroSection(): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CTASection and FooterSection</name>
  <files>
    components/sections/CTASection.tsx
    components/sections/FooterSection.tsx
    __tests__/components/sections/CTASection.test.tsx
    __tests__/components/sections/FooterSection.test.tsx
  </files>
  <read_first>
    __tests__/components/sections/CTASection.test.tsx
    __tests__/components/sections/FooterSection.test.tsx
    .planning/phases/04-static-content-sections/04-CONTEXT.md
  </read_first>
  <action>
**CTASection.tsx** — Server Component (no 'use client'):

```tsx
import { RevealWrapper } from '@/components/motion/RevealWrapper'

export function CTASection() {
  return (
    <section className="py-24 px-6 bg-dark relative overflow-hidden">
      <div className="max-w-7xl mx-auto relative z-10">
        <RevealWrapper variant="fadeUp">
          <h2 className="font-switzer text-4xl md:text-5xl font-bold text-white leading-tight max-w-xl">
            Transforme Suas Ideias em Visuais de Outro Nivel
          </h2>
          <a
            href="/contato"
            className="inline-block mt-8 bg-gold text-white px-8 py-3 rounded-full font-switzer font-semibold text-sm tracking-wide hover:opacity-90 transition-opacity"
          >
            Entre em Contato
          </a>
        </RevealWrapper>
      </div>
      {/* Placeholder for standing figure — absolute positioned */}
      <div className="absolute bottom-0 right-12 w-48 h-72 bg-white/5 rounded-t-full hidden lg:block" />
    </section>
  )
}
```

Key details:
- `bg-dark` section for dark background contrast
- "Transforme Suas Ideias em Visuais de Outro Nivel" heading in white Switzer
- "Entre em Contato" gold button with `href="/contato"`
- Placeholder figure: `absolute bottom-0 right-12 w-48 h-72 bg-white/5 rounded-t-full hidden lg:block`
- RevealWrapper with fadeUp
- No 'use client'

**FooterSection.tsx** — Server Component (no 'use client'):

```tsx
export function FooterSection() {
  return (
    <footer className="px-6 pb-6 bg-bg">
      <div className="bg-white rounded-3xl px-8 py-12 md:px-12 md:py-16">
        <div className="max-w-7xl mx-auto">
          {/* Top: brand + tagline */}
          <div className="mb-12">
            <h2 className="font-switzer text-3xl font-bold text-dark">Corazon</h2>
            <p className="text-dark/50 text-sm mt-2">
              Uma contadora de historias visual — fotografia e videografia que capturam a essencia.
            </p>
          </div>

          {/* Middle: nav links + social */}
          <div className="flex flex-col md:flex-row justify-between gap-8 mb-12">
            {/* Explorar nav links */}
            <div>
              <h3 className="font-switzer font-semibold text-sm text-dark/40 uppercase tracking-widest mb-4">
                Explorar
              </h3>
              <nav className="flex flex-col gap-2">
                <a href="/" className="text-dark/70 text-sm hover:text-dark transition-colors">Inicio</a>
                <a href="/portfolio" className="text-dark/70 text-sm hover:text-dark transition-colors">Portfolio</a>
                <a href="/contato" className="text-dark/70 text-sm hover:text-dark transition-colors">Contato</a>
              </nav>
            </div>

            {/* Social links */}
            <div>
              <h3 className="font-switzer font-semibold text-sm text-dark/40 uppercase tracking-widest mb-4">
                Social
              </h3>
              <div className="flex gap-4">
                <a href="https://x.com" target="_blank" rel="noopener noreferrer" className="text-dark/70 text-sm hover:text-dark transition-colors">
                  X
                </a>
                <a href="https://instagram.com" target="_blank" rel="noopener noreferrer" className="text-dark/70 text-sm hover:text-dark transition-colors">
                  Instagram
                </a>
              </div>
            </div>

            {/* Legal */}
            <div>
              <h3 className="font-switzer font-semibold text-sm text-dark/40 uppercase tracking-widest mb-4">
                Legal
              </h3>
              <a href="/privacidade" className="text-dark/70 text-sm hover:text-dark transition-colors">
                Privacidade
              </a>
            </div>
          </div>

          {/* Bottom: credit + copyright */}
          <div className="border-t border-dark/10 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
            <p className="text-dark/40 text-xs">Criado por Markus Corazzione</p>
            <p className="text-dark/40 text-xs">MC. &copy; 2026</p>
          </div>
        </div>
      </div>
    </footer>
  )
}
```

Key details:
- `<footer>` element (semantic HTML), not `<section>`
- White card: `bg-white rounded-3xl px-8 py-12 md:px-12 md:py-16` inside `px-6 pb-6 bg-bg`
- "Corazon" brand in Switzer bold
- Portuguese tagline
- "Explorar" nav: Inicio, Portfolio, Contato
- Social: X and Instagram as text links (no SVG icons — text placeholders per CONTEXT.md)
- Legal: Privacidade link
- "Criado por Markus Corazzione" and "MC. &copy; 2026" in bottom bar
- No 'use client' — pure Server Component

**Update test files:**

CTASection.test.tsx:
- Uncomment import
- Test "Transforme" heading text present: `screen.getByText(/Transforme Suas Ideias/i)`
- Test "Entre em Contato" link with href="/contato": `screen.getByRole('link', { name: /Entre em Contato/i })` then check `getAttribute('href')` equals `/contato`

FooterSection.test.tsx:
- Uncomment import
- Test "Corazon" brand: `screen.getByText(/Corazon/i)`
- Test copyright: `screen.getByText(/MC\..*2026/)`
- Test "Criado por Markus Corazzione": `screen.getByText(/Criado por Markus Corazzione/i)`
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/CTASection.test.tsx __tests__/components/sections/FooterSection.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "bg-dark" components/sections/CTASection.tsx returns match
    - grep "Transforme Suas Ideias" components/sections/CTASection.tsx returns match
    - grep 'href="/contato"' components/sections/CTASection.tsx returns match
    - grep "Entre em Contato" components/sections/CTASection.tsx returns match
    - grep "bg-gold" components/sections/CTASection.tsx returns match
    - grep "RevealWrapper" components/sections/CTASection.tsx returns match
    - grep "bg-white rounded-3xl" components/sections/FooterSection.tsx returns match
    - grep "Corazon" components/sections/FooterSection.tsx returns match
    - grep "Criado por Markus Corazzione" components/sections/FooterSection.tsx returns match
    - grep "MC\." components/sections/FooterSection.tsx returns match
    - grep "2026" components/sections/FooterSection.tsx returns match
    - grep "Explorar" components/sections/FooterSection.tsx returns match
    - grep "Instagram" components/sections/FooterSection.tsx returns match
    - Neither file contains 'use client'
    - Both test files pass
  </acceptance_criteria>
  <done>CTASection renders dark bg with heading and gold button to /contato. FooterSection renders white card with brand, socials, nav, credit, and copyright. Both are Server Components. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all sections into page.tsx + build verification</name>
  <files>
    app/page.tsx
  </files>
  <read_first>
    app/page.tsx
    components/sections/PhotoStripSection.tsx
    components/sections/FooterSection.tsx
  </read_first>
  <action>
Update `app/page.tsx` to import all 10 section components plus the existing HeroSection. Stack them in visual order. Add Phase 5 placeholder anchors.

**Complete file content:**

```tsx
import { HeroSection } from '@/components/hero/HeroSection'
import { PhotoStripSection } from '@/components/sections/PhotoStripSection'
import { ManifestoSection } from '@/components/sections/ManifestoSection'
import { ProofStripSection } from '@/components/sections/ProofStripSection'
import { ServicesSection } from '@/components/sections/ServicesSection'
import { PortfolioSection } from '@/components/sections/PortfolioSection'
import { BlogSection } from '@/components/sections/BlogSection'
import { FAQSection } from '@/components/sections/FAQSection'
import { CTASection } from '@/components/sections/CTASection'
import { FooterSection } from '@/components/sections/FooterSection'

export default function Home() {
  return (
    <main>
      <HeroSection />
      <PhotoStripSection />
      <ManifestoSection />
      <ProofStripSection />
      <ServicesSection />
      {/* Phase 5: Process sticky cards */}
      <section id="process" aria-hidden="true" />
      <PortfolioSection />
      <BlogSection />
      {/* Phase 5: Testimonial carousel */}
      <section id="testimonials" aria-hidden="true" />
      <FAQSection />
      <CTASection />
      <FooterSection />
    </main>
  )
}
```

Key details:
- `app/page.tsx` stays a Server Component — NO 'use client'
- 11 imports total: HeroSection + 10 new sections
- Phase 5 placeholders: `<section id="process" aria-hidden="true" />` and `<section id="testimonials" aria-hidden="true" />`
- Process placeholder goes after ServicesSection, before PortfolioSection
- Testimonials placeholder goes after BlogSection, before FAQSection
- FooterSection is last

Then run `npm run build` to verify the entire homepage compiles cleanly:

```bash
npm run build
```

If build fails, diagnose and fix — common issues:
- Missing 'use client' on a component that uses motion.div with whileHover
- Import path typos
- next/image with non-existent src (should not happen — we use placeholder divs)
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "HeroSection" app/page.tsx returns match
    - grep "PhotoStripSection" app/page.tsx returns match
    - grep "ManifestoSection" app/page.tsx returns match
    - grep "ProofStripSection" app/page.tsx returns match
    - grep "ServicesSection" app/page.tsx returns match
    - grep "PortfolioSection" app/page.tsx returns match
    - grep "BlogSection" app/page.tsx returns match
    - grep "FAQSection" app/page.tsx returns match
    - grep "CTASection" app/page.tsx returns match
    - grep "FooterSection" app/page.tsx returns match
    - grep 'id="process"' app/page.tsx returns match
    - grep 'id="testimonials"' app/page.tsx returns match
    - grep "aria-hidden" app/page.tsx returns match
    - app/page.tsx does NOT contain 'use client'
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>app/page.tsx imports and renders all 10 sections + HeroSection in order with Phase 5 placeholder anchors. npm run build passes cleanly.</done>
</task>

</tasks>

<verification>
cd "E:/Fotógrafo" && npx vitest run --reporter=verbose && npm run build
</verification>

<success_criteria>
- CTASection renders dark bg with heading and "Entre em Contato" gold button linking to /contato
- FooterSection renders white card with Corazon, social links, nav, credit, copyright
- page.tsx imports all 10 sections + HeroSection, has Phase 5 anchors
- page.tsx is a Server Component (no 'use client')
- All vitest tests pass (full suite)
- npm run build exits 0
- Complete homepage scrolls from hero through footer
</success_criteria>

<output>
After completion, create `.planning/phases/04-static-content-sections/04-03-SUMMARY.md`
</output>
