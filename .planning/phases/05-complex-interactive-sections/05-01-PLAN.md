---
phase: 05-complex-interactive-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/components/sections/ProcessSection.test.tsx
  - __tests__/components/sections/BentoSection.test.tsx
  - __tests__/components/sections/TestimonialSection.test.tsx
  - components/sections/ProcessSection.tsx
  - components/sections/BentoSection.tsx
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03

must_haves:
  truths:
    - "ProcessSection renders 6 sticky stacked cards with incrementing top offsets and alternating rotation"
    - "Each process card shows step number, step name, circular image placeholder, and description"
    - "BentoSection renders an asymmetric 3-column grid with tall left photo card spanning 2 rows"
    - "Bento center card shows Atenção aos Detalhes heading with 6 skill checklist items"
    - "Bento has 4 sub-cards: gear, quality polaroids, collaborations, and dark narrative card"
  artifacts:
    - path: "components/sections/ProcessSection.tsx"
      provides: "Sticky stacked process cards section"
      contains: "processSteps"
    - path: "components/sections/BentoSection.tsx"
      provides: "Asymmetric bento trust grid"
      contains: "Construído na Confiança"
    - path: "__tests__/components/sections/ProcessSection.test.tsx"
      provides: "Unit tests for process section"
      contains: "describe"
    - path: "__tests__/components/sections/BentoSection.test.tsx"
      provides: "Unit tests for bento section"
      contains: "describe"
    - path: "__tests__/components/sections/TestimonialSection.test.tsx"
      provides: "Unit test stubs for testimonial section"
      contains: "it.todo"
  key_links:
    - from: "components/sections/ProcessSection.tsx"
      to: "lib/data.ts"
      via: "import { processSteps }"
      pattern: "import.*processSteps.*from.*lib/data"
    - from: "components/sections/BentoSection.tsx"
      to: "tailwind.config.ts"
      via: "Tailwind tokens bg-dark, bg-gold, font-switzer"
      pattern: "bg-dark|bg-gold|font-switzer"
---

<objective>
Create Wave 0 test stubs for all three Phase 5 sections, then implement ProcessSection (sticky stacked cards) and BentoSection (asymmetric trust grid) as Server Components.

Purpose: Deliver the two non-interactive sections (pure CSS, no client JS) and establish test scaffolding for the entire phase.
Output: Two working Server Components + 3 test files (2 complete, 1 with todo stubs for Plan 02).
</objective>

<execution_context>
@E:/Fotógrafo/.claude/get-shit-done/workflows/execute-plan.md
@E:/Fotógrafo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-complex-interactive-sections/05-CONTEXT.md
@.planning/phases/05-complex-interactive-sections/05-RESEARCH.md

<interfaces>
<!-- From lib/data.ts -->
```typescript
export interface ProcessStep {
  id: number
  name: string
  description: string
  image: string
}

export const processSteps: ProcessStep[] = [
  { id: 1, name: 'Descoberta', description: 'Entender sua visão, objetivos e história antes de ligar a câmera.', image: '/images/placeholder.jpg' },
  { id: 2, name: 'Planejamento', description: 'Criando uma abordagem personalizada com painéis de humor, listas de cenas e cronogramas.', image: '/images/placeholder.jpg' },
  { id: 3, name: 'Captação', description: 'Capturando momentos com iluminação precisa, composição e emoção em foco.', image: '/images/placeholder.jpg' },
  { id: 4, name: 'Edição', description: 'Gradação de cores em cada imagem para revelar a história por trás delas.', image: '/images/placeholder.jpg' },
  { id: 5, name: 'Entrega', description: 'Apresentando arquivos de alta qualidade em formatos prontos para todas as plataformas.', image: '/images/placeholder.jpg' },
  { id: 6, name: 'Otimização', description: 'Revisando resultados e refinando o fluxo de trabalho para resultados ainda melhores.', image: '/images/placeholder.jpg' },
]

export interface Testimonial {
  id: string
  name: string
  role: string
  quote: string
  rating: number
  avatar: string
}

export const testimonials: Testimonial[] = [
  { id: 'testimonial-1', name: 'Danny Rose', role: 'Gerente de Marca, Solé Studio', quote: 'Visuais excepcionais que capturaram nossa marca perfeitamente.', rating: 5, avatar: '/images/placeholder.jpg' },
  { id: 'testimonial-2', name: 'David Nguyen', role: 'Diretor Criativo, FrameHaus', quote: 'Um processo incrivelmente fluido, resultados deslumbrantes. Verdadeiramente uma profissional.', rating: 5, avatar: '/images/placeholder.jpg' },
  { id: 'testimonial-3', name: 'Alisha Moore', role: 'Organizadora de Eventos, Moments & Co.', quote: 'Cada foto contou uma história. Não poderia pedir algo melhor.', rating: 5, avatar: '/images/placeholder.jpg' },
]
```

<!-- From __tests__/components/sections/ServicesSection.test.tsx — mock pattern -->
```typescript
const { mockUseReducedMotion } = vi.hoisted(() => ({
  mockUseReducedMotion: vi.fn(() => false),
}))

vi.mock('motion/react', () => ({
  motion: {
    div: ({ children, ...props }: React.HTMLAttributes<HTMLDivElement> & { children?: React.ReactNode }) => (
      <div {...props}>{children}</div>
    ),
  },
  AnimatePresence: ({ children }: { children: React.ReactNode }) => <>{children}</>,
  useReducedMotion: mockUseReducedMotion,
  useInView: vi.fn(() => true),
}))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test files for all three Phase 5 sections</name>
  <files>
    __tests__/components/sections/ProcessSection.test.tsx,
    __tests__/components/sections/BentoSection.test.tsx,
    __tests__/components/sections/TestimonialSection.test.tsx
  </files>
  <read_first>
    __tests__/components/sections/ServicesSection.test.tsx,
    lib/data.ts
  </read_first>
  <action>
Create three test files following the established mock pattern from ServicesSection.test.tsx.

**ProcessSection.test.tsx** — Full tests (component built in Task 2):
- Mock `motion/react` using the `vi.hoisted` + `vi.mock` pattern from ServicesSection.test.tsx
- Mock `@/components/motion/RevealWrapper` as passthrough div
- Mock `@/components/motion/StaggerChildren` as passthrough div (StaggerChildren + StaggerItem)
- Import `{ ProcessSection }` from `@/components/sections/ProcessSection`
- Tests:
  1. `renders section heading "Um Caminho Claro para Visuais Excepcionais"` — `screen.getByText('Um Caminho Claro para Visuais Excepcionais')`
  2. `renders 6 process step cards` — query all step names: Descoberta, Planejamento, Captacao, Edicao, Entrega, Otimizacao (use `getByText` for each — note: accented chars in data.ts, but test should match exact strings from data)
  3. `each card shows step number` — verify text `01` through `06` present (padStart format)
  4. `each card shows circular image placeholder` — use container query: all elements with class containing `rounded-full` OR use `querySelectorAll('[class*="rounded-full"]')` — expect 6
  5. `each card has sticky class` — use container query: `querySelectorAll('[class*="sticky"]')` — expect 6
  6. `cards have rotation classes` — verify container has elements with class containing `rotate-`

**BentoSection.test.tsx** — Full tests (component built in Task 2):
- Same mock pattern (motion/react, RevealWrapper, StaggerChildren)
- Import `{ BentoSection }` from `@/components/sections/BentoSection`
- Tests:
  1. `renders section heading` — `screen.getByText(/Construído na Confiança/)`
  2. `renders section description` — `screen.getByText(/Habilidade, precisão/)`
  3. `renders Visao Personalizada overlay text` — `screen.getByText('Visão Personalizada')`
  4. `renders Atencao aos Detalhes card with skill checklist` — `screen.getByText('Atenção aos Detalhes')` + verify 6 skill items: Iluminacao, Enquadramento, Cor, Composicao, Edicao, Som
  5. `renders equipment card` — `screen.getByText(/Captado com o Melhor Equipamento/)`
  6. `renders Qualidade Consistente card` — `screen.getByText('Qualidade Consistente')`
  7. `renders Colaboracoes Fluidas card` — `screen.getByText('Colaborações Fluidas')`
  8. `renders Abordagem Narrativa card` — `screen.getByText('Abordagem Narrativa')`

**TestimonialSection.test.tsx** — Stub tests only (component built in Plan 05-02):
- Same mock pattern
- `describe('TestimonialSection', () => { ... })` with `it.todo()` stubs:
  1. `it.todo('renders dark section with video element')`
  2. `it.todo('renders testimonial card with name and quote')`
  3. `it.todo('auto-advances after 5000ms')`
  4. `it.todo('dot click updates displayed card')`
  5. `it.todo('renders 3 dot navigation buttons')`
  6. `it.todo('respects reduced motion preference')`
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/TestimonialSection.test.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - __tests__/components/sections/ProcessSection.test.tsx contains `describe('ProcessSection'`
    - __tests__/components/sections/ProcessSection.test.tsx contains `vi.mock('motion/react'`
    - __tests__/components/sections/ProcessSection.test.tsx contains `sticky`
    - __tests__/components/sections/ProcessSection.test.tsx contains `rotate-`
    - __tests__/components/sections/BentoSection.test.tsx contains `describe('BentoSection'`
    - __tests__/components/sections/BentoSection.test.tsx contains `Visão Personalizada`
    - __tests__/components/sections/BentoSection.test.tsx contains `Atenção aos Detalhes`
    - __tests__/components/sections/BentoSection.test.tsx contains `Abordagem Narrativa`
    - __tests__/components/sections/TestimonialSection.test.tsx contains `it.todo(`
    - __tests__/components/sections/TestimonialSection.test.tsx contains `auto-advances`
  </acceptance_criteria>
  <done>All 3 test files exist. TestimonialSection tests are it.todo() stubs. ProcessSection and BentoSection tests exist but will fail until Task 2 creates the components.</done>
</task>

<task type="auto">
  <name>Task 2: Implement ProcessSection with pure CSS sticky stacking cards</name>
  <files>components/sections/ProcessSection.tsx</files>
  <read_first>
    lib/data.ts,
    index.html (lines 1050-1110 for process section HTML reference),
    components/sections/ServicesSection.tsx,
    components/motion/RevealWrapper.tsx
  </read_first>
  <action>
Create `components/sections/ProcessSection.tsx` as a Server Component (NO `'use client'` directive).

**Structure:**
- Import `{ processSteps }` from `@/lib/data`
- Import `{ RevealWrapper }` from `@/components/motion/RevealWrapper`
- Define lookup arrays as COMPLETE Tailwind string literals (not dynamically constructed — Tailwind purges dynamic classes):

```typescript
const topClasses = ['top-16', 'top-20', 'top-24', 'top-28', 'top-32', 'top-36']
const zClasses = ['z-[10]', 'z-[20]', 'z-[30]', 'z-[40]', 'z-[50]', 'z-[60]']
const rotations = [
  'rotate-[1deg]', 'rotate-[-0.5deg]', 'rotate-[0.5deg]',
  'rotate-[-1deg]', 'rotate-[0.5deg]', 'rotate-[-0.5deg]',
]
```

**Component JSX:**
- Outer `<section id="process" className="relative bg-bg py-24">`
- Inner container `<div className="max-w-2xl mx-auto px-6">`
- Wrap heading in `<RevealWrapper>`:
  - `<h2 className="font-switzer font-bold text-4xl text-dark mb-12 text-center">Um Caminho Claro para Visuais Excepcionais</h2>`
- Tall scroll container: `<div className="relative min-h-[300vh]">`
- Map over `processSteps` with `key={step.id}`:
  - Card wrapper: `<div className={\`sticky \${topClasses[i]} \${zClasses[i]} \${rotations[i]} mb-6 bg-white rounded-2xl p-8 shadow-lg transition-shadow\`}>`
  - Step number (top-right, gold): `<span className="absolute top-6 right-8 text-gold font-switzer font-bold text-xs tracking-widest">{String(step.id).padStart(2, '0')}</span>`
  - Circular image placeholder: `<div className="w-24 aspect-square rounded-full bg-dark/10 mx-auto mb-6" />`
  - Step name: `<h3 className="font-switzer font-bold text-xl text-dark text-center mb-3">{step.name}</h3>`
  - Description: `<p className="font-sans text-sm text-dark/60 text-center leading-relaxed">{step.description}</p>`

**Export:** Named export `export function ProcessSection()`

**CRITICAL:** No `'use client'` directive. No `motion/react` import. No `useScroll`. Pure CSS sticky stacking only.
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/ProcessSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/ProcessSection.tsx exists
    - components/sections/ProcessSection.tsx does NOT contain `'use client'`
    - components/sections/ProcessSection.tsx does NOT contain `useScroll`
    - components/sections/ProcessSection.tsx contains `import { processSteps } from '@/lib/data'`
    - components/sections/ProcessSection.tsx contains `export function ProcessSection`
    - components/sections/ProcessSection.tsx contains `sticky`
    - components/sections/ProcessSection.tsx contains `rotate-[1deg]`
    - components/sections/ProcessSection.tsx contains `rotate-[-0.5deg]`
    - components/sections/ProcessSection.tsx contains `min-h-[300vh]`
    - components/sections/ProcessSection.tsx contains `padStart(2, '0')`
    - components/sections/ProcessSection.tsx contains `rounded-full`
    - components/sections/ProcessSection.tsx contains `z-[10]`
    - ProcessSection.test.tsx passes: `npx vitest run __tests__/components/sections/ProcessSection.test.tsx` exits 0
  </acceptance_criteria>
  <done>ProcessSection renders 6 sticky stacked cards with incrementing top/z-index values, alternating rotation, step numbers (01-06), circular image placeholders, step names, and descriptions. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Implement BentoSection with asymmetric CSS Grid trust layout</name>
  <files>components/sections/BentoSection.tsx</files>
  <read_first>
    index.html (lines 1112-1198 for bento HTML structure),
    components/sections/ServicesSection.tsx,
    components/motion/RevealWrapper.tsx
  </read_first>
  <action>
Create `components/sections/BentoSection.tsx` as a Server Component (NO `'use client'` directive).

**Structure:**
- Import `{ RevealWrapper }` from `@/components/motion/RevealWrapper`
- Export named function `BentoSection`

**Section wrapper:**
- `<section id="why" className="bg-bg py-24">`
- Inner container: `<div className="max-w-6xl mx-auto px-6">`

**Heading area (wrapped in RevealWrapper):**
- `<h2 className="font-switzer font-bold text-3xl md:text-4xl text-dark mb-4">Construído na Confianca,<br />Movido pela Qualidade</h2>`
- `<p className="font-sans text-sm text-dark/60 max-w-[340px] leading-relaxed mb-12">Habilidade, precisao e foco claro em criar visuais que movem pessoas e marcas para frente.</p>`

Note: Use the exact accented Portuguese characters from index.html: "Confianca" -> "Confianca" with cedilla = "Confiança", "precisao" -> "precisão". Copy exact strings from index.html.

**Bento grid (wrapped in RevealWrapper):**
- `<div className="grid grid-cols-1 sm:grid-cols-3 gap-3">`

**6 cells in order matching index.html:**

1. **Vision card (tall, left, spans 2 rows):**
   `<div className="relative sm:row-span-2 rounded-2xl overflow-hidden min-h-[280px] sm:min-h-[500px]" style={{ background: 'linear-gradient(160deg, #2a1a10, #4a3020)' }}>`
   - Center placeholder: `<div className="w-full h-full flex items-center justify-center text-[120px] opacity-30">` eye emoji `</div>`
   - Overlay text: `<span className="absolute bottom-7 left-7 font-switzer font-bold text-white text-xl">Visao Personalizada</span>` (use exact: "Visão Personalizada")

2. **Detail card (center, beige):**
   `<div className="relative rounded-2xl bg-[#E8E0D2] p-8">`
   - Heading: `<h3 className="font-switzer font-bold text-xl text-dark mb-5">Atencao aos Detalhes</h3>` (use exact: "Atenção aos Detalhes")
   - Skill checklist: `<div className="flex flex-wrap gap-x-6 gap-y-3">` mapping over `['Iluminacao', 'Enquadramento', 'Cor', 'Composicao', 'Edicao', 'Som']` (use exact accented: `['Iluminação', 'Enquadramento', 'Cor', 'Composição', 'Edição', 'Som']`)
   - Each skill: `<span className="flex items-center gap-2 text-sm font-sans text-dark"><span className="text-gold">` checkmark `</span> {skill}</span>`
   - Decorative gold circles SVG in top-right corner (position absolute, opacity-15): 3 concentric circles with stroke="#C8973A"

3. **Gear card (right top, green-gray):**
   `<div className="rounded-2xl bg-[#C5CEC8] p-8 flex items-end min-h-[200px]">`
   - `<h3 className="font-switzer font-bold text-dark text-lg">Captado com o Melhor Equipamento</h3>`

4. **Quality card (polaroids):**
   `<div className="rounded-2xl bg-white p-8 flex flex-col items-center justify-center gap-2">`
   - Polaroid stack: 3 small divs with absolute positioning, slight rotations (`rotate-[-8deg]`, `rotate-[3deg]`, `rotate-[-2deg]`), white bg, shadow, containing a colored placeholder inner div
   - `<h3 className="font-switzer font-bold text-dark text-xl text-center">Qualidade Consistente</h3>`

5. **Collaborations card:**
   `<div className="rounded-2xl bg-white p-8">`
   - `<h3 className="font-switzer font-bold text-dark text-lg mb-3">Colaboracoes Fluidas</h3>` (use exact: "Colaborações Fluidas")
   - Avatar stack: 3 small circular placeholder divs (w-[30px] h-[30px] rounded-full) with different bg colors (#c8b8a8, #b8a898, #d8c8b8), overlapping via negative margin
   - Stars row: 5 gold star characters
   - Stats label: `<span className="text-xs font-sans text-dark/60">540+ Clientes no Mundo</span>`

6. **Story card (dark):**
   `<div className="rounded-2xl bg-[#4a2820] p-8 flex flex-col justify-end min-h-[200px]">`
   - `<h3 className="font-switzer font-bold text-white text-xl mb-2">Abordagem Narrativa</h3>`
   - `<p className="font-sans text-xs text-white/60 leading-relaxed">Foco em visuais que conectam e deixam uma impressao duradoura.</p>` (use exact: "impressão duradoura")

**CRITICAL:** No `'use client'` directive. No motion imports. Pure Server Component with CSS Grid layout. All Tailwind classes are complete string literals.
  </action>
  <verify>
    <automated>cd "E:/Fotógrafo" && npx vitest run __tests__/components/sections/BentoSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/BentoSection.tsx exists
    - components/sections/BentoSection.tsx does NOT contain `'use client'`
    - components/sections/BentoSection.tsx contains `export function BentoSection`
    - components/sections/BentoSection.tsx contains `Construído na Confiança`
    - components/sections/BentoSection.tsx contains `Visão Personalizada`
    - components/sections/BentoSection.tsx contains `Atenção aos Detalhes`
    - components/sections/BentoSection.tsx contains `Captado com o Melhor Equipamento`
    - components/sections/BentoSection.tsx contains `Qualidade Consistente`
    - components/sections/BentoSection.tsx contains `Colaborações Fluidas`
    - components/sections/BentoSection.tsx contains `Abordagem Narrativa`
    - components/sections/BentoSection.tsx contains `sm:row-span-2`
    - components/sections/BentoSection.tsx contains `sm:grid-cols-3`
    - BentoSection.test.tsx passes: `npx vitest run __tests__/components/sections/BentoSection.test.tsx` exits 0
  </acceptance_criteria>
  <done>BentoSection renders the full asymmetric 3-column grid with all 6 cells matching index.html structure. Left vision card spans 2 rows, center detail card has skill checklist, right column has gear/quality/collabs/story sub-cards. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run __tests__/components/sections/ProcessSection.test.tsx` exits 0
- `npx vitest run __tests__/components/sections/BentoSection.test.tsx` exits 0
- `npx vitest run __tests__/components/sections/TestimonialSection.test.tsx` exits 0 (todo stubs pass)
- `npx vitest run` full suite still green (no regressions)
</verification>

<success_criteria>
- ProcessSection.tsx is a Server Component rendering 6 sticky cards with rotation, step numbers, placeholders, and descriptions
- BentoSection.tsx is a Server Component rendering the asymmetric bento grid with all 6 cells
- All 3 test files exist; ProcessSection and BentoSection tests pass; TestimonialSection has todo stubs
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-complex-interactive-sections/05-01-SUMMARY.md`
</output>
